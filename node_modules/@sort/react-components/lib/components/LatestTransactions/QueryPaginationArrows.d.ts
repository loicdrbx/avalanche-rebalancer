interface QueryPaginationArrowsProps {
    theme: string;
    offset: number;
    total: number;
    loading: boolean;
    increaseOffset: () => void;
    decreaseOffset: () => void;
}
declare const QueryPaginationArrows: ({ theme, offset, total, loading, increaseOffset, decreaseOffset, }: QueryPaginationArrowsProps) => import("react/jsx-runtime").JSX.Element;
export default QueryPaginationArrows;
