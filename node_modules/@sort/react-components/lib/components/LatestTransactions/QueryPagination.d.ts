interface QueryPaginationProps {
    theme: string;
    offset: number;
    total: number;
    loading: boolean;
    increaseOffset: () => void;
    decreaseOffset: () => void;
}
declare const QueryPagination: ({ offset, total, theme, loading, increaseOffset, decreaseOffset, }: QueryPaginationProps) => import("react/jsx-runtime").JSX.Element;
export default QueryPagination;
