import { TransactionRow } from '../components/types/TransactionRow';
declare const useLatestTransactions: (contract_address: string, blockchain: string, offset: number, api_key: string, api_server?: string) => {
    rows: TransactionRow[];
    errorMsg: string;
    loading: boolean;
    queryCount: number;
};
export default useLatestTransactions;
